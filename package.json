{
  "name": "focus-files",
  "displayName": "Recently Focused Files",
  "description": "Tracks files you focused on",
  "version": "0.1.4",
  "publisher": "MarkSamios",
  "engines": {
    "vscode": "^1.112.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "categories": [
    "Other"
  ],
  "main": "./out/focus-files.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "focusFilesView",
          "name": "Focused Files"
        }
      ]
    },
    "commands": [
      {
        "command": "focusFiles.markFile",
        "title": "Pin in focused files",
        "icon": "$(star)"
      },
      {
        "command": "focusFiles.removeFile",
        "title": "Remove from Focused Files",
        "icon": "$(close)"
      },
      {
        "command": "focusFiles.removePosition",
        "title": "Remove Position",
        "icon": "$(close)"
      },
      {
        "command": "focusFiles.clearFiles",
        "title": "Clear Focused Files",
        "icon": "$(trash)"
      }
    ],
    "keybindings": [
      {
        "command": "focusFiles.markFile",
        "key": "ctrl+alt+f",
        "when": "editorTextFocus"
      }
    ],
    "menus": {
      "editor/title/context": [
        {
          "command": "focusFiles.markFile",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "focusFiles.markFile",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "focusFiles.markFile",
          "when": "explorerResourceIsFolder == false",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "focusFiles.removeFile",
          "when": "view == focusFilesView && viewItem == focusFiles",
          "group": "inline"
        },
        {
          "command": "focusFiles.removePosition",
          "when": "view == focusFilesView && viewItem == position",
          "group": "inline"
        },
        {
          "command": "focusFiles.markFile",
          "when": "view == focusFilesView && viewItem == focusFiles",
          "group": "2_mark"
        },
        {
          "command": "focusFiles.removeFile",
          "when": "view == focusFilesView && viewItem == focusFiles",
          "group": "3_remove_file"
        },
        {
          "command": "focusFiles.removePosition",
          "when": "view == focusFilesView && viewItem == position",
          "group": "3_remove_position"
        }
      ],
      "view/title": [
        {
          "command": "focusFiles.clearFiles",
          "when": "view == focusFilesView",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Focus Files",
      "properties": {
        "focusFiles.maxItems": {
          "type": "number",
          "default": 10,
          "description": "Maximum number of files to keep in the focused files list"
        },
        "focusFiles.maxPositionsPerFile": {
          "type": "number",
          "default": 5,
          "description": "Maximum number of positions to keep per file"
        },
        "focusFiles.minPreviewSize": {
          "type": "number",
          "default": 5,
          "description": "Minimum size of the preview label"
        },
        "focusFiles.maxPreviewSize": {
          "type": "number",
          "default": 120,
          "description": "Maximum size of the preview label"
        },
        "focusFiles.parentPathLevels": {
          "type": "number",
          "default": 3,
          "minimum": 0,
          "description": "Number of parent directories to show next to the file name."
        }
      }
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -p .",
    "watch": "tsc -w",
    "package": "vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "keywords": [
    "files",
    "focus",
    "productivity",
    "explorer",
    "quick access"
  ],
  "author": "Mark Samios",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/vscode": "^1.110.0",
    "@vscode/vsce": "^3.7.1",
    "typescript": "^5.9.3"
  },
  "funding": {
    "type": "github",
    "url": "https://github.com/sponsors/enigma1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/enigma1/focus-files.git"
  },
  "homepage": "https://github.com/enigma1/focus-files#readme",
  "bugs": {
    "url": "https://github.com/enigma1/focus-files/issues"
  },
  "icon": "images/icon.png"
}
